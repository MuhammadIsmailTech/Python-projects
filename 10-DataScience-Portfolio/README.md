# P-10-DataScience-Portfolio

Sample Data Science portfolio project.
Run `python -m venv .venv` then `pip install -r requirements.txt`.
Train model: `python src/train_model.py`
Run dashboard: `streamlit run dashboard/app.py`
Run API: `uvicorn api.server:app --reload`
